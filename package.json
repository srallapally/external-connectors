{
  "name": "external-connectors-pack",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "add-connector": "node ./scripts/add-connector.mjs",
    "clean": "node -e \"require('fs').rmSync('dist',{recursive:true,force:true})\""
  },
  "devDependencies": {
    "esbuild": "^0.25.10"
  }
}

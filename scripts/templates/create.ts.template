
  async function create(
    objectClass: string,
    attrs: Record<string, AttributeValue>,
    options?: OperationOptions
  ): Promise<ConnectorObject> {
    // TODO: Implement create operation
    logger.log(`Creating ${objectClass}`, attrs);

    switch (objectClass) {
{{#objectClassCase}}      case "{{objectClass}}": {
        // TODO: Create {{objectClass}} with attrs via your API/service
        // Example implementation:
        // const payload = {
        //   name: attrs.name,
        //   email: attrs.email,
        //   // ... map other attributes to API format
        // };
        // const response = await client.post('/{{objectClass}}', payload);
        // return {
        //   objectClass,
        //   uid: response.id,
        //   name: response.name,
        //   attributes: {
        //     id: response.id,
        //     name: response.name,
        //     // ... map response back to attributes
        //   }
        // };
        //
        // If the target refuses because the object already exists, throw
        // ConnectorError so the dispatcher can resolve this precisely
        // (FAILED_CONFIRMED, no wasted retries) instead of falling back to
        // its generic deadline-based classification:
        // if (isDuplicateError(err)) {
        //   throw new ConnectorError("ALREADY_EXISTS", `{{objectClass}} already exists`, { cause: err });
        // }

        // Placeholder implementation (replace with actual API call):
        const newUid = `${Date.now()}`;
        return {
          objectClass,
          uid: newUid,
          name: attrs.name as string,
          attributes: {
            ...attrs,
            id: newUid,
            // Add generated/returned attributes
          }
        };
      }
{{/objectClassCase}}      default:
        throw new Error(`Create not supported for ${objectClass}`);
    }
  }

